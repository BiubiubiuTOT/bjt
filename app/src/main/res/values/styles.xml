<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MatchMatch">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="AppTheme1" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/mine_bg</item>
        <item name="colorPrimary">@color/mine_bg</item>
        <item name="colorAccent">@color/mine_bg</item>
        <item name="colorControlActivated">@color/mine_bg</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="edit_AlertDialog_style" parent="@android:style/Theme.Dialog">
        <!--浮动-->
        <item name="android:windowIsFloating">false</item>
        <!--半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--无标题-->
        <item name="android:windowNoTitle">true</item>
        <!--系统背景颜色-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--当前背景颜色-->
        <item name="android:background">@android:color/transparent</item>
        <!--对话框背景色-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
